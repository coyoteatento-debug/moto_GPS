plugins {
    id "com.android.application"
    id "org.jetbrains.kotlin.android" // Debe decir esto exactamente
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace "com.example.moto_gps"
    compileSdk 34

    defaultConfig {
        applicationId "com.example.moto_gps"
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }
}
