plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace "com.example.moto_gps"
    compileSdk 34

    defaultConfig {
        minSdkVersion 21  // ✅ Mapbox requiere mínimo 21
        targetSdkVersion 34
    }
}
